services:
  web:
    build: .
    volumes:
      - ./tasks:/app/tasks
      - ./templates:/app/templates
      - ./static:/app/static
      - ./todo:/app/todo
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    command: python manage.py runserver 0.0.0.0:8000
